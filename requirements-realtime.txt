# Dependências adicionais para o Transcritor em Tempo Real
# Instale com: pip install -r requirements-realtime.txt

# Backend de áudio (escolha um):
sounddevice>=0.4.6  # Recomendado - melhor compatibilidade
# PyAudio        # Alternativa - pode ser difícil instalar no Windows

# Já deve estar em requirements.txt, mas listamos para garantir:
torch>=2.0.0
torchaudio>=2.0.0
pyannote.audio>=3.0.0
faster-whisper>=0.9.0
speechbrain>=0.5.14
huggingface_hub>=0.19.0
transformers>=4.30.0
spacy>=3.5.0
pydantic>=2.0.0
librosa>=0.10.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Utilitários opcionais:
# pytest  # Para testes
# jupyter # Para notebooks
